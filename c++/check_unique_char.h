// h file for Check Unique Char algorithm from Cracking Code interviews 

#include "practice.h"

class CheckUniqueChar
{
public:
  CheckUniqueChar();
  ~CheckUniqueChar();
  bool solver_boolean(std::string);
  bool solver_sorted(std::string);
  bool test_case();
}; 
